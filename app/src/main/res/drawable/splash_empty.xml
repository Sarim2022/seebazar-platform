<?xml version="1.0" encoding="utf-8"?>
<!-- Invisible placeholder so the system splash shows no visible icon (Compose splash is the real UI). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <size
        android:width="1dp"
        android:height="1dp" />
    <solid android:color="@android:color/transparent" />
</shape>
